# Publish now

From the directory that contains this repository folder:

```bash
cd after-the-crash-oratlas
bash scripts/publish_github.sh
```

The script validates all structured evidence, uses the existing public repository
`LaurentHuzard/after-the-crash-oratlas`, commits the project, pushes `main`, and creates release
`v0.1.0`.
